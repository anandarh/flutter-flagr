name: flutter_flagr
description: This is an unofficial Flagr Client SDK for Flutter. Flagr is a
  feature flagging, A/B testing and dynamic configuration microservice.
version: 0.1.0
author: R Herdianto
homepage: https://github.com/anandarh/flutter-flagr

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  ffcache: ^0.1.3
  http: ^0.12.2

dev_dependencies:
  flutter_test:
    sdk: flutter
